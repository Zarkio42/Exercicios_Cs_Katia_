using System;
using System.Collections.Generic;
using System.Dynamic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Aula_Katia_16_05
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Exercícios de hoje
            //Fazer um programa que armazena peso de 30 bois em uma matriz 5x6, após armazenar, mostre o
            //peso e a posição do boi mais pesado e do boi mais leve, e a média de peso de todos os bois.

            /*
            double[,] peso = new double[5, 6];
            var count = 1;

            for (int i = 0; i < peso.GetLength(0); i++)
            {
                for (int j = 0; j < peso.GetLength(1); j++)
                {
                    Console.WriteLine($"Digite o peso do {count}º boi");
                    peso[i, j] = Convert.ToInt32(Console.ReadLine());
                    count++;
                }
            }

            

            Console.WriteLine($"O boi {position} com {peso.GetEnumerator()} é o mais pesado");



            Console.ReadKey();

            */
        }
    }
}
